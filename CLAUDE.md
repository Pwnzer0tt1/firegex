# CLAUDE.md

**Read [AGENTS.md](AGENTS.md).** It holds everything: what this is, the commands, the
architecture, and the decisions worth not undoing.

Nothing is repeated here on purpose. This file existed as a full copy of that one for a
while, and by the time anybody noticed the two had drifted — the copy still described an
interface rule that had been rewritten and a protocol picker that had been redesigned —
with nothing to say which was current. One document, one place to edit it.

Anything genuinely specific to one agent belongs in that agent's own file. So far nothing
has been.
